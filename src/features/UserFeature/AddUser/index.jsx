import React from 'react';
import PropTypes from 'prop-types';

AddUser.propTypes = {
    
};

function AddUser(props) {
    return (
        <div>
            add user
        </div>
    );
}

export default AddUser;